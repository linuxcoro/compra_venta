<pre>
{error}<br>

{#x}
	{@traduce name="{name}" /}<br>
	{@traduce name="{date-added}" /}<br>
	{@traduce name="{priority}" /}<br>

	{@gt key=new-price.length value="10"}
		{@convertir valor="{new-price}" /}<br>
	{:else}
		{@convertir2 valor="{new-price}" /}<br>
	{/gt}
	<img src={picture}>
{/x}
